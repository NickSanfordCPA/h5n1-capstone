"""Model wrappers — baseline (non-sentiment) and +sentiment variants.

Time series (ARIMAX, SARIMAX, XGBoost) and ML (LSTM, GRU). Includes the permutation
test: shuffle the sentiment column, re-fit, compare — the core of the thesis.
"""
